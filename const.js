 const skillSingle = document.getElementById('skill-single');
  const skillsGrid = document.getElementById('skills-grid');
  const skillLogo = document.getElementById('skill-logo');
  const skillName = document.getElementById('skill-name');
  const skillDesc = document.getElementById('skill-desc');
  const skillProgress = document.getElementById('skill-progress');
  const skipAnimation = document.getElementById('skip-animation');